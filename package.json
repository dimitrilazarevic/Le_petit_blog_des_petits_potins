{
	"name": "basic-login-app",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"client": "vite dev",
		"server": "npm run server --prefix src/lib/server",
		"build": "vite build",
		"preview": "vite preview",
		"dev": "concurrently \"npm run client\" \"npm run server\""
	},
	"devDependencies": {
		"@sveltejs/adapter-auto": "^3.0.0",
		"@sveltejs/kit": "^2.0.0",
		"@sveltejs/vite-plugin-svelte": "^3.0.0",
		"svelte": "^4.2.7",
		"vite": "^5.0.3"
	},
	"type": "module",
	"dependencies": {
		"concurrently": "^8.2.2"
	}
}
